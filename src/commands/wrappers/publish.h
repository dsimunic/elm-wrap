#ifndef WRAPPER_PUBLISH_H
#define WRAPPER_PUBLISH_H

#include <stdbool.h>

/* Publish command entry point (deprecated - redirects to package publish) */
int cmd_publish(int argc, char *argv[]);

#endif /* WRAPPER_PUBLISH_H */
